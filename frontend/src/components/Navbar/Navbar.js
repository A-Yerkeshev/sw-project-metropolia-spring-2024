import styles from './Navbar.module.css';

export const Navbar = () => {
    return (
        <div className={styles.base}>
            <h1>Feedback App</h1>
        </div>
    );
}

export default Navbar;