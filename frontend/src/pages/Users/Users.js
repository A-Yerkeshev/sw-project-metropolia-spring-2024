import styles from './Users.module.css';

const Users = () => {
    return(
        <div className={styles.main}>
            <p className={styles.headtext}>Users attendance</p>
            <p className={styles.subheadtext}>Manage the users in your network.</p>
            <div>

            </div>
        </div>
    );
  }

export default Users;